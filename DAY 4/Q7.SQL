/*Many books are now available in multiple editions.

The library wants to record the Edition Number of each book in future.

Update the database structure to support this feature.*/
ALTER TABLE BOOKS
ADD ED_NUM VARCHAR2(100);
